@model IEnumerable<AidCare_The_Last_Aid.Models.donationtest>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.donationDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DonationAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.donationDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DonationAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.donationtestId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.donationtestId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.donationtestId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
